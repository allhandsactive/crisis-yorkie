<%= form_for([:admin, @vote]) do |f| %>
  <% if @vote.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@vote.errors.count, "error") %> prohibited this vote from being saved:</h2>

      <ul>
      <% @vote.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :voter_id %><br />
    <%= f.text_field :voter_id %>
  </div>
  <div class="field">
    <%= f.label :vote_ordinal %><br />
    <%= f.text_field :vote_ordinal %>
  </div>
  <div class="field">
    <%= f.label :decision_id %><br />
    <%= f.text_field :decision_id %>
  </div>
  <div class="field">
    <%= f.label :value %><br />
    <%= f.text_field :value %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
