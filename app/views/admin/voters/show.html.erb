<p id="notice"><%= notice %></p>

<% f = nil; form_for(:admin, Voter.new) {|ff| f = ff } # @todo hack %>

<% @voter.class.ui_fields.map(&:intern).each do |k| %>
  <p>
    <b><%= f.label(k) %>:</b>
    <%= @voter.send(k) %>
  </p>
<% end %>

<%= link_to 'Edit', edit_admin_voter_path(@voter) %> |
<%= link_to 'Back', admin_voters_path %>
